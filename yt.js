function getMessage() {
    const message = document.getElementById("userMessage").value;
    console.log("User entered:", message);
    // You can now do anything with the message, like display or send to server
}
